spring:
  application:
    # 服务名称和配置文件保持一致
    name: user-service
  profiles:
    # 环境也是和配置文件保持一致
    active: dev
  cloud:
    nacos:
      discovery:
        # 配置Nacos注册中心地址
        server-addr: localhost:8848
      config:
        namespace: f888c235-c983-4181-8fc9-99a361212a51
        file-extension: yml
        server-addr: localhost:8848
    sentinel:
      transport:
        # 添加监控页面地址即可
        dashboard: localhost:8858




